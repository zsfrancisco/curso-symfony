{% extends 'base.html.twig' %}

{% block title %}New Asignatura{% endblock %}

{% block body %}
    <h1>Create new Asignatura</h1>

    {{ include('asignatura/_form.html.twig') }} <hr>

    <a class="btn btn-primary" href="{{ path('asignatura_index') }}">Volver al listado</a>
    <a class="btn btn-primary" href="{{ path('panel') }}">Home</a>
{% endblock %}
