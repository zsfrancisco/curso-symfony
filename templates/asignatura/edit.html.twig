{% extends 'base.html.twig' %}

{% block title %}Edit Asignatura{% endblock %}

{% block body %}
    <h1>Edit Asignatura</h1>

    {{ include('asignatura/_form.html.twig', {'button_label': 'Actualizar'}) }} <hr>

    <a class="btn btn-primary" href="{{ path('asignatura_index') }}">Volver al listado</a>
    <a class="btn btn-primary" href="{{ path('panel') }}">Home</a> <hr>

    {{ include('asignatura/_delete_form.html.twig') }}
{% endblock %}
