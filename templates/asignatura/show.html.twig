{% extends 'base.html.twig' %}

{% block title %}Asignatura{% endblock %}

{% block body %}
    <h1>Asignatura</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ asignatura.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ asignatura.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('asignatura_index') }}">Volver al listado</a>

    <a class="btn btn-primary" href="{{ path('asignatura_edit', {'id': asignatura.id}) }}">editar</a>
    <a class="btn btn-primary" href="{{ path('panel') }}">Home</a> <hr>

    {{ include('asignatura/_delete_form.html.twig') }}
{% endblock %}
