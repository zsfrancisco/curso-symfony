{% extends 'base.html.twig' %}

{% block title %}Estudiante{% endblock %}

{% block body %}
    <h1>Estudiante</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ estudiante.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ estudiante.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('estudiante_index') }}">Volver al listado</a>

    <a class="btn btn-primary" href="{{ path('estudiante_edit', {'id': estudiante.id}) }}">editar</a>
    <a class="btn btn-primary" href="{{ path('panel') }}">Home</a><hr>

    {{ include('estudiante/_delete_form.html.twig') }}
{% endblock %}
