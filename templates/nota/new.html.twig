{% extends 'base.html.twig' %}

{% block title %}New Nota{% endblock %}

{% block body %}
    <h1>Create new Nota</h1>

    {{ include('nota/_form.html.twig') }}<hr>

    <a class=" btn btn-primary " href="{{ path('nota_index') }}">Volver al listado</a>
    <a class=" btn btn-primary " href="{{ path('panel') }}">Home</a>
{% endblock %}
