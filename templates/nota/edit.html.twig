{% extends 'base.html.twig' %}

{% block title %}Edit Nota{% endblock %}

{% block body %}
    <h1>Edit Nota</h1>

    {{ include('nota/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('nota_index') }}">back to list</a>
    <a href="{{ path('panel') }}">Home</a>

    {{ include('nota/_delete_form.html.twig') }}
{% endblock %}
